#include "holberton.h"
/**
   * size - check the code for Holberton School students.
    *print_triangle
     * Return: Always 0.
      */
void print_triangle(int size)
{
	int x
		if (size == 0)
			_putchar('\n');
for (x = 0; x <= 9; x++)
 if
	 (size == x)
		 _putchar('#');
}
